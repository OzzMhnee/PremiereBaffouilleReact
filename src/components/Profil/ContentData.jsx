

export const contentData = () => {

    return(
        <div>
            <ul>
                array.forEach(element => {
                    <li>{element.name} - {element.age} ans</li>
                });
            </ul>
        </div>
    );
}